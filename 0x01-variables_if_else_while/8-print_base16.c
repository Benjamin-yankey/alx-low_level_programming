#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char h;

	int d;

	h = 'a';
	d = 0;
	while
		(d < 10) {
			putchar(d + '0');
			d++;
		}
	while 
		(h <= 'f') {
			putchar(h);
			h++;
		}
	putchar('\n');
	return (0);
}
